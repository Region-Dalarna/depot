name: Deploy

on:
  push:
  branches: [main]

jobs:
  deploy:
  runs-on: [self-hosted, "{{runner_label}}"]
steps:
  - uses: actions/checkout@v4

- name: Render
run: quarto render

- name: Synka till servern
run: |
  mkdir -p "{{malsokvag}}"
rsync -a --delete _site/ "{{malsokvag}}/"